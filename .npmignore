# These are excluded from the npm published package.
# The private GitHub repo (via .gitignore) tracks these for personal cross-machine use.
# Public npx users build their own database using the ingestion scripts.

# Raw data — excluded from npm, tracked in private GitHub
pdfs/

# Source TypeScript — only dist/ is needed by consumers
src/
*.ts

# Stale compiled files from deleted TypeScript sources — not needed by consumers
dist/scraper.js
dist/ingest.js
dist/ingest-pdfs.js
dist/migrate-legacy.js
dist/test_setup.js
dist/test-client.js
dist/chunker.js

# Dev & test artifacts
node_modules/
.env
.cache
*.log
tests/
test-*.js
test-*.mjs

# CI / dev config
.github/
.gitattributes
.gitignore
tsconfig.json
.tmp-pdfs/
